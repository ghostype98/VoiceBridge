{
  "name": "voicebridge-frontend",
  "version": "1.0.0",
  "description": "VoiceBridge 语音面试前端（与主服务同端口，默认 8002，以 config/config.yaml 为准）",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
